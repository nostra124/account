---
id: AUDIT-2026-05-13
type: audit
kind: post-milestone
status: done
---

# AUDIT-2026-05-13 — post-ROADMAP-1.4.0 and post-ROADMAP-1.5.0

## Scope

Per `skills/audit.md` §1, a post-milestone audit is mandatory
on every milestone close. Two milestones have closed since
AUDIT-2026-05-12:

- **ROADMAP-1.4.0** (merged at `e949e43`) — milestone +
  audit institutions; corpus housekeeping.
- **ROADMAP-1.5.0** (merged at `fc5ca78`) — packaging
  completion; closed FEAT-023.

Plus the standalone PR #7 (`8a6e529`, no ROADMAP) that
tightened the auto-merge contract to webhook-driven
subscribe + end-turn.

This audit covers everything shipped since
`2026-05-12-first-audit` and runs the same five-input walk
defined in `skills/audit.md` §2.

## Method

For each artifact under `bin/`, `tests/unit/`,
`share/man/`, `etc/bash_completion.d/`, `.github/workflows/`,
`skills/`, `tests/pre-push.sh`, `tests/ci-post-failure.sh`,
`.githooks/`, and `Makefile.in`: locate the owning
`FEAT-` / `BUG-` via `git log --oneline -- <path>` and
match commit messages to issue ids.

## Findings

### Clean (OK)

- `skills/milestones.md` → **FEAT-216** (merged `e949e43`).
- `skills/audit.md` → **FEAT-217** (merged `e949e43`).
- `issues/audits/2026-05-12-first-audit.md` → **FEAT-217**
  first-application (`e949e43`).
- The 15 issues moved into `done/` and the three deleted
  ROADMAP files in `e949e43` → **FEAT-218**.
- `etc/bash_completion.d/account` (rewritten) → **FEAT-219**
  (merged `fc5ca78`); two unit tests pin the contract.
- `docs/templates/CLAUDE.md.account` → **FEAT-220**
  (merged `fc5ca78`); unit test pins existence + reference.
- `FEAT-023` archived in `issues/feature/done/` — six ACs
  verified; the audit-findings section in its body cites
  this audit and AUDIT-2026-05-12.

### MISSING-ISSUE — institution work from `3870056` (PR #6 squash)

Commit `3870056` ("review: tests, runtime fixes, CI feedback
channel, agent institutions") bundled the test-review FEATs
(208-215, BUG-002…005, FEAT-211 exit-code pinning) with a
large block of process-and-tooling additions that had **no
owning FEAT** at the time. The unowned artifacts:

1. `skills/testing.md`
2. `skills/logging.md`
3. `skills/bugs.md`
4. `skills/features.md`
5. `skills/automerging.md`
6. `tests/pre-push.sh`
7. `tests/ci-post-failure.sh`
8. `.githooks/pre-push`
9. `Makefile.in` `pre-push` / `install-hooks` targets
10. `.github/workflows/ci.yml` — `pull-requests: write`
    permission and the per-job failure-comment posting
11. `bin/account` `error()` helper (non-exiting counterpart
    to `fatal`)
12. `bin/account` format-unification of `warn` / `info`
    (return 0; consistent ANSI placement)
13. `bin/account` `ACCOUNT_SOURCE_ONLY=1` source-only mode
14. The 9 new logging unit tests (`debug`/`info`/`warn`/
    `error`/`fatal` + source-only marker)

PR #7 (`8a6e529`) later tightened items 1, 5, and §11 of
CLAUDE.md to the webhook-driven contract — also without
an owning FEAT.

**Action:** file two backfill FEATs, both `status: done`
since the work has shipped:

- **FEAT-221** — agent institutions (items 1-10 above, plus
  PR #7's tightening). Goes straight into
  `issues/feature/done/221-agent-institutions.md`.
- **FEAT-222** — logger error level + source-only test mode
  (items 11-14). Goes straight into
  `issues/feature/done/222-error-level-and-source-only-mode.md`.

### ORPHAN-ROADMAP — `issues/ROADMAP-1.5.0.md`

`ROADMAP-1.5.0.md` was marked `status: done` in the PR that
closed it (`fc5ca78`) but the file itself remained in the
working tree. Per `skills/milestones.md` §2 Close, the
delete step belongs to "after the PR merges to master" —
which means the next PR is responsible. That's this audit
PR.

**Action:** `git rm issues/ROADMAP-1.5.0.md`. History at
`fc5ca78` preserves the record.

### MISSING-COVERAGE — none

Every open issue has a matching artifact and every artifact
walked above has either an owning FEAT or a queued
backfill FEAT.

### STALE — none

Every `status: done` issue lives in the appropriate `done/`
subdirectory. The open issue corpus is empty after FEAT-023
closure.

## Actions taken

In this audit's PR:

1. File `FEAT-221` directly into
   `issues/feature/done/221-agent-institutions.md` with
   `status: done`. Body cites this audit + commit
   `3870056` + commit `8a6e529`.
2. File `FEAT-222` directly into
   `issues/feature/done/222-error-level-and-source-only-mode.md`
   with `status: done`. Body cites this audit + commit
   `3870056`.
3. `git rm issues/ROADMAP-1.5.0.md`.

## Carried forward

None. After this audit:

- 0 open `FEAT-` issues
- 0 open `BUG-` issues
- 0 open `ROADMAP-` files
- 18 archived features (was 17 + FEAT-221 + FEAT-222 = 19)
- 5 archived bugs

The corpus is clean. The next planning cycle starts from
zero open items; whatever the maintainer wants to ship next
becomes the seed of `ROADMAP-1.6.0` (or `1.5.1` if it's a
patch-only bug fix).
